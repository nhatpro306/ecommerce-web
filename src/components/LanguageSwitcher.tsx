"use client";

// Language switching is intentionally disabled until full i18n is in place.
// The previous EN button only translated a handful of nav labels which
// gave customers a broken experience. We keep the file (and the i18n hook)
// so the rest of the app can still call useI18n() safely.

export function LanguageSwitcher() {
  return null;
}

